{
  "name": "@penpot/renderer-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "types:check": "tsc --noEmit -p tsconfig.json",
    "start:noop": "pnpm run build && node /Volumes/fushilu/.caches/penpot/renderer-service/index.js",
    "test": "pnpm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^5.8.3"
  }
}
